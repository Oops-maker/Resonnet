# Deprecated

This directory is deprecated. Moderator modes have been migrated to the unified format:

**New location**: `skills/moderator_modes/`

See `skills/moderator_modes/default/` for the current structure (meta.json, moderator_common.md, standard.md, etc.).

The runtime now loads from `skills/moderator_modes/` exclusively. This directory is kept for reference only.
