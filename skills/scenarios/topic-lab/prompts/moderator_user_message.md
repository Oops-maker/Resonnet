User requirements: {user_prompt}

Please generate only the role-specific moderator prompt (role, Goal, Phases). Use the unified format: Role line → ## Goal → ## Phases with **Phase name** (round descriptor): guidance. Avoid hardcoded numbers. Do NOT include Workspace, Experts, Rules, or Language — these are appended by the system at runtime.
